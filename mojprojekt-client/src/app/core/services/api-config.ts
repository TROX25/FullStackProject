// Points at MojProjekt.Api's default "http" launch profile (see Properties/launchSettings.json).
// Adjust if you run the backend on a different port/profile.
export const API_BASE_URL = 'http://localhost:5025/api';
